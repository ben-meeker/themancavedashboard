# The Man Cave Dashboard - Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# FILE PATHS
# ============================================

# Path to config directory containing:
#   - config.json (dashboard configuration)
#   - credentials.json (Google OAuth credentials for Calendar widget)
#   - token.json (Google OAuth token for Calendar widget)
#   - photos/ (photo carousel images)
#   - All other files/folders required by widgets
#
# Note: Widgets can customize filenames/folders in their widget config (config.json)
# Example: Calendar widget can use "google_credentials_filename" in its config
#          Photos widget can use "photos_folder" in its config
#
# (default: ./config)
# This directory is mounted into the container to prevent bind mount issues
CONFIG_DIR=./config

# ============================================
# REDIS (Managed by Docker Compose)
# ============================================

# Redis connection URL (automatically set by docker-compose)
# REDIS_URL=redis://redis:6379

# ============================================
# WIDGET API KEYS & SETTINGS
# ============================================
#
# Note: Most widget settings should be in config.json (per widget instance)
# Environment variables here are for:
# - API keys and credentials (sensitive data)
# - Optional fallback values when not specified in widget config
#

# Tesla Widget (via Tessie API)
# Get your API key from: https://my.tessie.com/settings/api
TESSIE_API_KEY=your_tessie_api_key_here
TESSIE_VIN=your_tesla_vin_here

# Weather Widget (OpenWeatherMap)
# Get your API key from: https://openweathermap.org/api
# Note: Location (lat/lon) is configured per widget in config.json
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Plant Sensors Widget (Ecowitt)
# Get your keys from: https://www.ecowitt.net/
ECOWITT_API_KEY=your_ecowitt_api_key_here
ECOWITT_APPLICATION_KEY=your_ecowitt_application_key_here
ECOWITT_GATEWAY_MAC=your_gateway_mac_address_here

# Traeger Grill Widget
# Use your Traeger app login credentials
TRAEGER_USERNAME=your_traeger_email@example.com
TRAEGER_PASSWORD=your_traeger_password

# ============================================
# OPTIONAL FALLBACK VALUES
# ============================================
# These are optional fallbacks if not specified in widget config

# Meal Calendar Widget - iCal URL
# Can also be set per widget in config.json as "calendar_url"
# MEAL_ICAL_URL=https://your-meal-app.com/calendar.ics

# ============================================
# NOTES FOR WIDGET DEVELOPERS
# ============================================

# When implementing Redis caching in widgets:
# - Use a unique prefix for your widget's keys (e.g., "tesla:", "weather:", "photos:")
# - Example: redis.Set(ctx, "tesla:battery_level", value, expiration)
# - This prevents key collisions between widgets
# - Access Redis via the REDIS_URL environment variable

# When adding new widgets:
# 1. API keys and credentials go in .env (this file)
# 2. Widget-specific settings go in config.json (per widget instance)
# 3. Files/folders required by widgets go in CONFIG_DIR
# 4. Document all requirements in your widget's README.md
#
# Example widget config structure in config.json:
# {
#   "id": "mywidget",
#   "location": { "x": 0, "y": 0 },
#   "config": {
#     "setting1": "value1",
#     "custom_filename": "my-data.json"  // file in CONFIG_DIR
#   }
# }

